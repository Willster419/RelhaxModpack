using System.Drawing;
using System.Drawing.Imaging;

namespace RelhaxModpack.AtlasesCreator
{
    /**
    public class DdsImageExporter : IImageExporter
    {
        
        internal int dwMagic { get; } = 0x20534444;
        internal int dwsSize = 124;
        internal int dwFlags = 0x0000000;
        internal int dwHeight = 0;              // height of image
        internal int dwWidth = 0;               // width of image
        internal int dwLinearSize = 0;          // texture size
        // internal byte[] Buffer1: array[0..59] of byte; // not important parameters
        // internal TMagicChars dwFourCC: TMagicChars; // 'DXT1' or 'DXT5'
        // internal byte[] Buffer2: array[0..39] of byte; // not important parameters

        public string ImageExtension
        {
            get { return "dds"; }
        }

        public void Save(string filename, Bitmap image)
        {
            // image.Save(filename, ImageFormat.Bmp);
        }

        internal CreateHeader(Bitmap image)
        {

            dwMagic = 
        } 
    }**/
}
