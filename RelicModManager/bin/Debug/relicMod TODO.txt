(normal) comment code and put in log outputs

(enhancement)* add /silent-config option auto install with specific config prefrences
  -must have config file
  -must have settings file

(enhancement) optimize and condense code to be better before official release

(optional) add first time loading info stuff

(optional) add /test switch acceptor
  
(optional) have it uninstall only the mods installed
  -save the old patch folders somehow
  -keep a log of every extracted zip entry
  delete every file in the log (if it exists)
  -recursivly delete each folder if the number of files and folders in it is 0
  -read each patch file and reverse the patch