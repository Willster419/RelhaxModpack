version 18.61:
- Added more mods (mostly ingame catagory, and xvm)
- fixed bug with regex patcher
- fixed bug with mod selection of enabling an optional checkbox
- added "file" mode for regex replacement, looks through entire file
rather than line by line